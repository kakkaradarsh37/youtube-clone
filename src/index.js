import React from "react";
import  ReactDOM  from "react-dom/client";
import "./index.css";
import App from "./App"

const root = ReactDOM.createRoot(document.getElementById("root"));//inside the root we will render all the pages 
root.render(<App/>);//one time code